<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color of the shape -->
    <corners android:radius="128dp" /> <!-- Radius of the rounded corners -->
    <stroke
        android:width="1dp"
    android:color="@color/secondary_text" /> <!-- Border color -->
</shape>
